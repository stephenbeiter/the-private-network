<div class="modal-content">
    <div class="row">
        <div class="col s8 push-s2">
            <!-- Dropdown Trigger -->
            <a class='dropdown-trigger btn' href='#' data-target='dropdown1'>Select
                Group</a>
            <!-- Dropdown Structure -->
            <ul id='dropdown1' class='dropdown-content'>
                {{!-- accessing an array from a partial --}}
                {{#each this}}
                <li><a href="#!">{{groupname}}</a></li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>